---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-ngx-data
  namespace: default
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi